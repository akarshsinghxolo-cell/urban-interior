name: Vendor cleanup runner marker

on:
  workflow_dispatch:

jobs:
  marker:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Application CI performs the canonical Vendor transformation on the cleanup PR."
