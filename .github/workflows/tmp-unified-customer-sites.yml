name: Apply unified Customer and Sites cleanup

on:
  pull_request:
    branches: [main]
    types: [opened, reopened, synchronize]

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.repo.full_name == github.repository && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14
      - name: Remove dead Customer and Site paths
        run: python scripts/cleanup-unified-customer-sites-legacy.py
      - name: Keep workflow changes for connector commit
        run: git restore .github/workflows/application-ci.yml .github/workflows/tmp-unified-customer-sites.yml
      - name: Install and validate focused behavior
        run: |
          bun install --frozen-lockfile
          bun run test:customer-sites-save
          bun run test:customer-sites-legacy
          bun run test:phase2b
          bun run build
      - name: Commit cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src tests scripts/cleanup-unified-customer-sites-legacy.py
          git diff --cached --quiet && exit 0
          git commit -m "Remove legacy Customer and Site write paths"
          git push origin HEAD:${{ github.head_ref }}
