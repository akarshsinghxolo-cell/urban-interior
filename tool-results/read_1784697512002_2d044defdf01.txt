     1→     1→/**
     2→     2→ * Contractors slice — contractor master records, contractor bids (quotation
     3→     3→ * award), contractor settlement, RA bills (running account bills), contractor
     4→     4→ * payment requests/approvals/recording, and partner commission accrual/pay.
     5→     5→ *
     6→     6→ * Phase 3h moved the 12 contractor actions out of store.ts. The shared
     7→     7→ * helpers `addDays` and `contractorPaymentProofStatus` were moved to
     8→     8→ * `../helpers`, and `canonicalPaymentEvent`, `materializePaymentSchedule`,
     9→     9→ * `eventMatchesPaymentTrigger` were moved to `../finance-helpers`, so this
    10→    10→ * slice can import them directly without duplicating logic.
    11→    11→ */
    12→    12→import type {
    13→    13→    Contractor, ContractorBid, ContractorSettlement,
    14→    14→    ContractorBill, ContractorPayment, Commission,
    15→    15→    WorkOrder, WorkOrderCostLine,
    16→    16→} from "../../types";
    17→    17→import type { ContractorsState } from "../types";
    18→    18→import type { StoreContext } from "../context";
    19→    19→import { assertRole, genId, nowIso, today, addDays, contractorPaymentProofStatus } from "../helpers";
    20→    20→import { formatINR } from "../../format";
    21→    21→import { assertWorkOrderRelations } from "../../business-rules";
    22→    22→import { materializePaymentSchedule } from "../finance-helpers";
    23→    23→// I: Import the canonical commission-rule lookup helper exposed by Agent A
    24→    24→// in masters.ts. Match priority: (1) partner-specific category rule →
    25→    25→// (2) partner-specific workOrder rule → (3) partner-specific all rule →
    26→    26→// (4) global all rule. Falls back to partner.commission_pct || 5 if no rule
    27→    27→// matches. Keeping this in lock-step with the master UI banner documented by
    28→    28→// Agent A in MastersSalesOpsModule.
    29→    29→import { findCommissionRule } from "./masters";
    30→    30→
    31→    31→export function createContractorsSlice(ctx: StoreContext): ContractorsState {
    32→    32→    const { commitState, get } = ctx;
    33→    33→
    34→    34→    return {
    35→    35→        addContractor: (c) => {
    36→    36→            const id = c.id || genId("con");
    37→    37→            const contractor: import("../../types").Contractor = {
    38→    38→                id,
    39→    39→                name: c.name || "New contractor",
    40→    40→                phone: c.phone,
    41→    41→                city: c.city,
    42→    42→                locality: c.locality,
    43→    43→                address: c.address,
    44→    44→                trade: c.trade,
    45→    45→                rating: c.rating,
    46→    46→                active_jobs: 0,
    47→    47→                outstanding: 0,
    48→    48→                reliability_score: c.reliability_score,
    49→    49→                on_time_pct: c.on_time_pct,
    50→    50→                past_jobs_count: c.past_jobs_count || 0,
    51→    51→                specializations: c.specializations || [],
    52→    52→                latitude: c.latitude,
    53→    53→                longitude: c.longitude,
    54→    54→                photo_attachment_id: c.photo_attachment_id,
    55→    55→                business_card_attachment_id: c.business_card_attachment_id,
    56→    56→                reliability_rating: c.reliability_rating,
    57→    57→                politeness_rating: c.politeness_rating,
    58→    58→                worker_count_range: c.worker_count_range,
    59→    59→                deadline_commitment: c.deadline_commitment,
    60→    60→                source_partner_id: c.source_partner_id,
    61→    61→                source_partner_name: c.source_partner_name,
    62→    62→                work_capabilities: c.work_capabilities || [],
    63→    63→            };
    64→    64→            commitState((s: any) => ({
    65→    65→                db: {
    66→    66→                    ...s.db,
    67→    67→                    master: {
    68→    68→                        ...s.db.master,
    69→    69→                        contractors: [contractor, ...s.db.master.contractors],
    70→    70→                    },
    71→    71→                },
    72→    72→            }));
    73→    73→            get().logAudit({
    74→    74→                actor: get().currentUser().name,
    75→    75→                actor_role: get().currentUser().role,
    76→    76→                action: `Created contractor "${contractor.name}"`,
    77→    77→                entity_type: "contractor",
    78→    78→                entity_id: id,
    79→    79→                kind: "create",
    80→    80→            });
    81→    81→            return id;
    82→    82→        },
    83→    83→
    84→    84→        updateContractor: (id, patch) => {
    85→    85→            commitState((s: any) => ({
    86→    86→                db: {
    87→    87→                    ...s.db,
    88→    88→                    master: {
    89→    89→                        ...s.db.master,
    90→    90→                        contractors: s.db.master.contractors.map((c: any) => c.id === id ? { ...c, ...patch } : c),
    91→    91→                    },
    92→    92→                },
    93→    93→            }));
    94→    94→            const actor_c = get().currentUser();
    95→    95→            get().logAudit({
    96→    96→                actor: actor_c.name,
    97→    97→                actor_role: actor_c.role,
    98→    98→                action: `Updated contractor ${id}`,
    99→    99→                entity_type: "contractor",
   100→   100→                entity_id: id,
   101→   101→                entity_label: patch.name || id,
   102→   102→                kind: "update",
   103→   103→                reason: `Edited by ${actor_c.name} (${actor_c.role})`,
   104→   104→            });
   105→   105→        },
   106→   106→
   107→   107→        addContractorBid: (b) => {
   108→   108→            const state = get();
   109→   109→            const id = b.id || genId("bid");
   110→   110→            const acceptedScope = state.db.acceptedScopes.find((scope: any) => scope.id === b.accepted_scope_id) ||
   111→   111→                (b.work_order_id
   112→   112→                    ? state.db.acceptedScopes.find((scope: any) => state.db.workOrders
   113→   113→                        .find((workOrder: any) => workOrder.id === b.work_order_id)
   114→   114→                        ?.accepted_scope_ids.includes(scope.id))
   115→   115→                    : undefined);
   116→   116→            const contractor = state.db.master.contractors.find((candidate: any) => candidate.id === b.contractor_id);
   117→   117→            if (!acceptedScope || !contractor)
   118→   118→                return "";
   119→   119→            // CV-14: Coerce undefined/null/NaN quote_amount to a finite number (default 0).
   120→   120→            // The UI used to pass `quote_amount: undefined` (JobBiddingBody) which silently propagated
   121→   121→            // through selectContractorBid as `contractor_award_amount: undefined` and rendered as "—" forever.
   122→   122→            const coercedQuote = Number.isFinite(b.quote_amount) && (b.quote_amount as number) >= 0
   123→   123→                ? (b.quote_amount as number)
   124→   124→                : 0;
   125→   125→            const workOrder = acceptedScope.work_order_id
   126→   126→                ? state.db.workOrders.find((row: any) => row.id === acceptedScope.work_order_id)
   127→   127→                : undefined;
   128→   128→            const work = state.db.workRequired.find((row: any) => row.id === acceptedScope.work_required_id);
   129→   129→            const now = nowIso();
   130→   130→            const bidNo = `CB-2026-${String(state.db.contractorBids.length + 1).padStart(3, "0")}`;
   131→   131→            const target = workOrder?.work_order_no || `Scope · ${acceptedScope.label}`;
   132→   132→            const threadId = state.openThreadFor("bid", id, `Contractor bid ${bidNo} · ${target}`, [contractor.name]);
   133→   133→            const bid: ContractorBid = {
   134→   134→                id,
   135→   135→                bid_no: bidNo,
   136→   136→                accepted_scope_id: acceptedScope.id,
   137→   137→                work_order_id: workOrder?.id,
   138→   138→                work_order_no: workOrder?.work_order_no || "Pending contractor award",
   139→   139→                site_id: acceptedScope.site_id,
   140→   140→                contractor_id: contractor.id,
   141→   141→                contractor_name: contractor.name,
   142→   142→                scope: b.scope || work?.title || acceptedScope.label,
   143→   143→                quote_amount: coercedQuote,
   144→   144→                rate_basis: b.rate_basis,
   145→   145→                estimated_days: b.estimated_days,
   146→   146→                with_material: b.with_material,
   147→   147→                reliability_score: contractor.reliability_score ?? b.reliability_score,
   148→   148→                on_time_pct: contractor.on_time_pct ?? b.on_time_pct,
   149→   149→                past_jobs_count: contractor.past_jobs_count ?? b.past_jobs_count,
   150→   150→                rating: contractor.rating ?? b.rating,
   151→   151→                evaluation_notes: b.evaluation_notes,
   152→   152→                status: "submitted",
   153→   153→                submitted_at: now,
   154→   154→                thread_id: threadId,
   155→   155→                created_at: now,
   156→   156→                updated_at: now,
   157→   157→            };
   158→   158→            commitState((s: any) => ({
   159→   159→                db: { ...s.db, contractorBids: [bid, ...s.db.contractorBids] },
   160→   160→            }));
   161→   161→            get().logAudit({
   162→   162→                actor: get().currentUser().name,
   163→   163→                actor_role: get().currentUser().role,
   164→   164→                action: `Contractor bid ${bidNo} submitted by ${contractor.name} for ${target}`,
   165→   165→                entity_type: "bid",
   166→   166→                entity_id: id,
   167→   167→                entity_label: bidNo,
   168→   168→                kind: "create",
   169→   169→                cross_post: [
   170→   170→                    { entity_type: "contractor", entity_id: contractor.id, entity_label: contractor.name },
   171→   171→                    ...(bid.work_order_id ? [{ entity_type: "workOrder", entity_id: bid.work_order_id, entity_label: bid.work_order_no }] : []),
   172→   172→                    ...(bid.site_id ? [{ entity_type: "site", entity_id: bid.site_id }] : []),
   173→   173→                    ...(workOrder?.customer_id ? [{ entity_type: "customer", entity_id: workOrder.customer_id }] : []),
   174→   174→                ],
   175→   175→            });
   176→   176→            return id;
   177→   177→        },
   178→   178→
   179→   179→        updateContractorBid: (id, patch) => commitState((s: any) => ({
   180→   180→            db: {
   181→   181→                ...s.db,
   182→   182→                contractorBids: s.db.contractorBids.map((bid: any) => bid.id === id ? { ...bid, ...patch, updated_at: nowIso() } : bid),
   183→   183→            },
   184→   184→        })),
   185→   185→
   186→   186→        selectContractorBid: (bidId) => {
   187→   187→            const state = get();
   188→   188→            const bid = state.db.contractorBids.find((row: any) => row.id === bidId);
   189→   189→            const acceptedScope = bid?.accepted_scope_id
   190→   190→                ? state.db.acceptedScopes.find((scope: any) => scope.id === bid.accepted_scope_id)
   191→   191→                : undefined;
   192→   192→            if (!bid || !acceptedScope)
   193→   193→                return;
   194→   194→            // CV-1 / CV-14: Reject awarding a bid with no real quote amount. Otherwise the work order's
   195→   195→            // contractor_award_amount becomes 0/undefined, every subsequent RA bill throws "exceeds the
   196→   196→            // contractor award / Work Order value", and the contractor payment chain dies entirely.
   197→   197→            if (!Number.isFinite(bid.quote_amount) || (bid.quote_amount as number) <= 0) {
   198→   198→                throw new Error("Cannot award a bid with no quote amount. Edit the bid to record the contractor's actual quote first.");
   199→   199→            }
   200→   200→            const now = nowIso();
   201→   201→            const existingWorkOrder = acceptedScope.work_order_id
   202→   202→                ? state.db.workOrders.find((row: any) => row.id === acceptedScope.work_order_id)
   203→   203→                : undefined;
   204→   204→            const workOrderId = existingWorkOrder?.id || genId("workOrder");
   205→   205→            const workOrderNo = existingWorkOrder?.work_order_no ||
   206→   206→                `WO-2026-${String(state.db.workOrders.length + 1).padStart(3, "0")}`;
   207→   207→            const quotation = state.db.quotations.find((quote: any) => quote.id === acceptedScope.quotation_id);
   208→   208→            const site = state.db.sites.find((row: any) => row.id === acceptedScope.site_id);
   209→   209→            const work = state.db.workRequired.find((row: any) => row.id === acceptedScope.work_required_id);
   210→   210→            const workOrder: WorkOrder = existingWorkOrder || {
   211→   211→                id: workOrderId,
   212→   212→                work_order_no: workOrderNo,
   213→   213→                customer_id: acceptedScope.customer_id,
   214→   214→                accepted_scope_ids: [acceptedScope.id],
   215→   215→                work_required_ids: [acceptedScope.work_required_id],
   216→   216→                quotation_ids: [acceptedScope.quotation_id],
   217→   217→                site_id: acceptedScope.site_id,
   218→   218→                area_ids: acceptedScope.area_ids,
   219→   219→                title: work?.title || acceptedScope.label,
   220→   220→                status: "scheduled",
   221→   221→                contractor_id: bid.contractor_id,
   222→   222→                contractor_name: bid.contractor_name,
   223→   223→                with_material: bid.with_material,
   224→   224→                material_responsibility: bid.with_material ? "contractor" : "company",
   225→   225→                contractor_award_amount: bid.quote_amount,
   226→   226→                // B-2: Formal bid path — tag selection_method="bid" so downstream
   227→   227→                // knows the price was competitively vetted.
   228→   228→                contractor_selection_method: "bid",
   229→   229→                start_date: today(),
   230→   230→                expected_end: bid.estimated_days
   231→   231→                    ? addDays(today(), bid.estimated_days)
   232→   232→                    : undefined,
   233→   233→                value: acceptedScope.accepted_value,
   234→   234→                progress: 0,
   235→   235→                site_address: site?.address,
   236→   236→                thread_id: state.openThreadFor("workOrder", workOrderId, `${workOrderNo} · ${work?.title || acceptedScope.label}`, [bid.contractor_name]),
   237→   237→                created_at: now,
   238→   238→                updated_at: now,
   239→   239→            };
   240→   240→            assertWorkOrderRelations(state.db, workOrder, "Work Order");
   241→   241→            commitState((s: any) => ({
   242→   242→                db: {
   243→   243→                    ...s.db,
   244→   244→                    contractorBids: s.db.contractorBids.map((row: any) => {
   245→   245→                        if (row.id === bidId)
   246→   246→                            return {
   247→   247→                                ...row,
   248→   248→                                status: "selected" as const,
   249→   249→                                work_order_id: workOrderId,
   250→   250→                                work_order_no: workOrderNo,
   251→   251→                                selected_at: now,
   252→   252→                                updated_at: now,
   253→   253→                            };
   254→   254→                        if (row.accepted_scope_id === acceptedScope.id &&
   255→   255→                            row.status === "submitted")
   256→   256→                            return {
   257→   257→                                ...row,
   258→   258→                                status: "rejected" as const,
   259→   259→                                rejected_at: now,
   260→   260→                                updated_at: now,
   261→   261→                            };
   262→   262→                        return row;
   263→   263→                    }),
   264→   264→                    acceptedScopes: s.db.acceptedScopes.map((scope: any) => scope.id === acceptedScope.id
   265→   265→                        ? {
   266→   266→                            ...scope,
   267→   267→                            status: "in_work_order" as const,
   268→   268→                            contractor_bid_id: bidId,
   269→   269→                            contractor_selection_method: "bid" as const,
   270→   270→                            work_order_id: workOrderId,
   271→   271→                        }
   272→   272→                        : scope),
   273→   273→                    workOrders: existingWorkOrder
   274→   274→                        ? s.db.workOrders.map((row: any) => row.id === workOrderId
   275→   275→                            ? {
   276→   276→                                ...row,
   277→   277→                                contractor_id: bid.contractor_id,
   278→   278→                                contractor_name: bid.contractor_name,
   279→   279→                                with_material: bid.with_material,
   280→   280→                                material_responsibility: bid.with_material
   281→   281→                                    ? "contractor"
   282→   282→                                    : "company",
   283→   283→                                contractor_award_amount: bid.quote_amount,
   284→   284→                                contractor_selection_method: "bid" as const,
   285→   285→                                updated_at: now,
   286→   286→                            }
   287→   287→                            : row)
   288→   288→                        : [workOrder, ...s.db.workOrders],
   289→   289→                    quotations: s.db.quotations.map((quote: any) => quote.id === acceptedScope.quotation_id
   290→   290→                        ? {
   291→   291→                            ...quote,
   292→   292→                            work_order_ids: Array.from(new Set([...quote.work_order_ids, workOrderId])),
   293→   293→                            updated_at: now,
   294→   294→                        }
   295→   295→                        : quote),
   296→   296→                    workRequired: s.db.workRequired.map((row: any) => row.id === acceptedScope.work_required_id
   297→   297→                        ? { ...row, status: "awarded" as const, updated_at: now }
   298→   298→                        : row),
   299→   299→                },
   300→   300→            }));
   301→   301→            if (!existingWorkOrder) {
   302→   302→                get().createBOQ(workOrderId);
   303→   303→                (quotation?.payment_terms || []).forEach((term: any) => {
   304→   304→                    const schedule = materializePaymentSchedule(term, quotation!, workOrder);
   305→   305→                    get().addPayment({
   306→   306→                        customer_id: acceptedScope.customer_id,
   307→   307→                        site_id: acceptedScope.site_id,
   308→   308→                        quotation_id: acceptedScope.quotation_id,
   309→   309→                        work_order_id: workOrderId,
   310→   310→                        amount: Math.round((acceptedScope.accepted_value * term.percentage) / 100),
   311→   311→                        status: "pending",
   312→   312→                        milestone_term_id: term.id,
   313→   313→                        milestone_label: `${term.label} ${term.percentage}%`,
   314→   314→                        due_event: schedule.due_event,
   315→   315→                        schedule_state: schedule.schedule_state,
   316→   316→                        due_date: schedule.due_date,
   317→   317→                    });
   318→   318→                });
   319→   319→                // B-2: Accrue partner commission when the customer came through a source partner.
   320→   320→                // The accrueCommission function already exists in this slice but was never called — so
   321→   321→                // partner commissions were never accrued on work-order creation, and the Commissions
   322→   322→                // module showed nothing. Now we trigger it whenever the awarded work order's customer
   323→   323→                // has a source_partner_id. Site-level partner attribution (Site.source_partner_id) is
   324→   324→                // also honoured as a fallback.
   325→   325→                const customer = state.db.customers.find((row: any) => row.id === acceptedScope.customer_id);
   326→   326→                const siteRow = state.db.sites.find((row: any) => row.id === acceptedScope.site_id);
   327→   327→                const partnerId = customer?.source_partner_id || siteRow?.source_partner_id;
   328→   328→                if (partnerId && quotation) {
   329→   329→                    try {
   330→   330→                        get().accrueCommission(workOrderId, quotation.id, partnerId);
   331→   331→                    } catch (err) {
   332→   332→                        // Don't block the award if commission accrual fails (e.g. partner removed mid-flight).
   333→   333→                        console.warn("accrueCommission failed:", err);
   334→   334→                    }
   335→   335→                }
   336→   336→            }
   337→   337→            const actor_bid = get().currentUser();
   338→   338→            get().addThreadReply(bid.thread_id || "", {
   339→   339→                author: actor_bid.name,
   340→   340→                role: actor_bid.role,
   341→   341→                body: `Bid selected — ${bid.contractor_name} awarded ${workOrderNo}.`,
   342→   342→                kind: "decision",
   343→   343→            });
   344→   344→            get().logAudit({
   345→   345→                actor: get().currentUser().name,
   346→   346→                actor_role: get().currentUser().role,
   347→   347→                action: `Awarded ${bid.contractor_name}: ${workOrderNo}`,
   348→   348→                entity_type: "bid",
   349→   349→                entity_id: bidId,
   350→   350→                entity_label: bid.bid_no,
   351→   351→                kind: "decision",
   352→   352→                source_module: "contractors",
   353→   353→                // Cross-post: bid selection is relevant to the Contractor, Customer, Site, and Work Order.
   354→   354→                cross_post: [
   355→   355→                    { entity_type: "contractor", entity_id: bid.contractor_id, entity_label: bid.contractor_name },
   356→   356→                    { entity_type: "workOrder", entity_id: workOrderId, entity_label: workOrderNo },
   357→   357→                    ...(acceptedScope.site_id ? [{ entity_type: "site", entity_id: acceptedScope.site_id }] : []),
   358→   358→                    { entity_type: "customer", entity_id: acceptedScope.customer_id },
   359→   359→                ],
   360→   360→            });
   361→   361→        },
   362→   362→
   363→   363→        directAwardContractor: (input) => {
   364→   364→            assertRole(get().currentUser().role, ["Owner", "Operations Manager"], "direct-award contractors");
   365→   365→            const state = get();
   366→   366→            const actor = state.currentUser();
   367→   367→            const trimmedReason = (input.award_reason || "").trim();
   368→   368→            if (!trimmedReason)
   369→   369→                throw new Error("A direct-award reason is required so the exception is auditable.");
   370→   370→            if (!input.contractor_id)
   371→   371→                throw new Error("Contractor is required for a direct award.");
   372→   372→            if (!input.accepted_scope_id)
   373→   373→                throw new Error("Accepted scope is required for a direct award.");
   374→   374→            const acceptedScope = state.db.acceptedScopes.find((scope: any) => scope.id === input.accepted_scope_id);
   375→   375→            if (!acceptedScope)
   376→   376→                throw new Error("Accepted scope not found.");
   377→   377→            if (acceptedScope.status === "cancelled")
   378→   378→                throw new Error("This accepted scope is cancelled.");
   379→   379→            const contractor = state.db.master.contractors.find((c: any) => c.id === input.contractor_id);
   380→   380→            if (!contractor)
   381→   381→                throw new Error("Contractor not found in master.");
   382→   382→            const now = nowIso();
   383→   383→            const existingWorkOrder = acceptedScope.work_order_id
   384→   384→                ? state.db.workOrders.find((row: any) => row.id === acceptedScope.work_order_id)
   385→   385→                : undefined;
   386→   386→            const workOrderId = existingWorkOrder?.id || genId("workOrder");
   387→   387→            const workOrderNo = existingWorkOrder?.work_order_no ||
   388→   388→                `WO-2026-${String(state.db.workOrders.length + 1).padStart(3, "0")}`;
   389→   389→            const quotation = state.db.quotations.find((quote: any) => quote.id === acceptedScope.quotation_id);
   390→   390→            const site = state.db.sites.find((row: any) => row.id === acceptedScope.site_id);
   391→   391→            const work = state.db.workRequired.find((row: any) => row.id === acceptedScope.work_required_id);
   392→   392→            const awardAmount = input.award_amount ?? acceptedScope.accepted_value;
   393→   393→            const workOrder: WorkOrder = existingWorkOrder || {
   394→   394→                id: workOrderId,
   395→   395→                work_order_no: workOrderNo,
   396→   396→                customer_id: acceptedScope.customer_id,
   397→   397→                accepted_scope_ids: [acceptedScope.id],
   398→   398→                work_required_ids: [acceptedScope.work_required_id],
   399→   399→                quotation_ids: [acceptedScope.quotation_id],
   400→   400→                site_id: acceptedScope.site_id,
   401→   401→                area_ids: acceptedScope.area_ids,
   402→   402→                title: work?.title || acceptedScope.label,
   403→   403→                status: "scheduled",
   404→   404→                contractor_id: contractor.id,
   405→   405→                contractor_name: contractor.name,
   406→   406→                with_material: input.with_material ?? false,
   407→   407→                material_responsibility: input.with_material ? "contractor" : "company",
   408→   408→                contractor_award_amount: awardAmount,
   409→   409→                contractor_selection_method: "direct_award",
   410→   410→                contractor_award_reason: trimmedReason,
   411→   411→                contractor_award_approved_by: actor.name,
   412→   412→                start_date: today(),
   413→   413→                expected_end: input.estimated_days ? addDays(today(), input.estimated_days) : undefined,
   414→   414→                value: acceptedScope.accepted_value,
   415→   415→                progress: 0,
   416→   416→                site_address: site?.address,
   417→   417→                thread_id: state.openThreadFor("workOrder", workOrderId, `${workOrderNo} · ${work?.title || acceptedScope.label}`, [contractor.name]),
   418→   418→                created_at: now,
   419→   419→                updated_at: now,
   420→   420→            };
   421→   421→            assertWorkOrderRelations(state.db, workOrder, "Work Order");
   422→   422→            commitState((s: any) => ({
   423→   423→                db: {
   424→   424→                    ...s.db,
   425→   425→                    acceptedScopes: s.db.acceptedScopes.map((scope: any) => scope.id === acceptedScope.id
   426→   426→                        ? {
   427→   427→                            ...scope,
   428→   428→                            status: "in_work_order" as const,
   429→   429→                            work_order_id: workOrderId,
   430→   430→                            contractor_selection_method: "direct_award" as const,
   431→   431→                        }
   432→   432→                        : scope),
   433→   433→                    workOrders: existingWorkOrder
   434→   434→                        ? s.db.workOrders.map((row: any) => row.id === workOrderId
   435→   435→                            ? {
   436→   436→                                ...row,
   437→   437→                                contractor_id: contractor.id,
   438→   438→                                contractor_name: contractor.name,
   439→   439→                                with_material: input.with_material ?? false,
   440→   440→                                material_responsibility: input.with_material ? "contractor" : "company",
   441→   441→                                contractor_award_amount: awardAmount,
   442→   442→                                contractor_selection_method: "direct_award" as const,
   443→   443→                                contractor_award_reason: trimmedReason,
   444→   444→                                contractor_award_approved_by: actor.name,
   445→   445→                                updated_at: now,
   446→   446→                            }
   447→   447→                            : row)
   448→   448→                        : [workOrder, ...s.db.workOrders],
   449→   449→                    quotations: s.db.quotations.map((quote: any) => quote.id === acceptedScope.quotation_id
   450→   450→                        ? {
   451→   451→                            ...quote,
   452→   452→                            work_order_ids: Array.from(new Set([...quote.work_order_ids, workOrderId])),
   453→   453→                            updated_at: now,
   454→   454→                        }
   455→   455→                        : quote),
   456→   456→                    workRequired: s.db.workRequired.map((row: any) => row.id === acceptedScope.work_required_id
   457→   457→                        ? { ...row, status: "awarded" as const, updated_at: now }
   458→   458→                        : row),
   459→   459→                },
   460→   460→            }));
   461→   461→            if (!existingWorkOrder) {
   462→   462→                get().createBOQ(workOrderId);
   463→   463→                (quotation?.payment_terms || []).forEach((term: any) => {
   464→   464→                    const schedule = materializePaymentSchedule(term, quotation!, workOrder);
   465→   465→                    get().addPayment({
   466→   466→                        customer_id: acceptedScope.customer_id,
   467→   467→                        site_id: acceptedScope.site_id,
   468→   468→                        quotation_id: acceptedScope.quotation_id,
   469→   469→                        work_order_id: workOrderId,
   470→   470→                        amount: Math.round((acceptedScope.accepted_value * term.percentage) / 100),
   471→   471→                        status: "pending",
   472→   472→                        milestone_term_id: term.id,
   473→   473→                        milestone_label: `${term.label} ${term.percentage}%`,
   474→   474→                        due_event: schedule.due_event,
   475→   475→                        schedule_state: schedule.schedule_state,
   476→   476→                        due_date: schedule.due_date,
   477→   477→                    });
   478→   478→                });
   479→   479→            }
   480→   480→            get().addThreadReply(workOrder.thread_id || "", {
   481→   481→                author: actor.name,
   482→   482→                role: actor.role,
   483→   483→                body: `Direct-award to ${contractor.name} (no formal bid round). Reason: "${trimmedReason}". ${input.note || ""}`,
   484→   484→                kind: "decision",
   485→   485→            });
   486→   486→            get().logAudit({
   487→   487→                actor: actor.name,
   488→   488→                actor_role: actor.role,
   489→   489→                action: `Direct-award contractor ${contractor.name} for ${workOrderNo} — reason: "${trimmedReason}"`,
   490→   490→                entity_type: "workOrder",
   491→   491→                entity_id: workOrderId,
   492→   492→                entity_label: workOrderNo,
   493→   493→                kind: "decision",
   494→   494→                reason: trimmedReason,
   495→   495→                source_module: "contractors",
   496→   496→                // Cross-post: direct award is relevant to the Contractor, Customer, and Site.
   497→   497→                cross_post: [
   498→   498→                    { entity_type: "contractor", entity_id: contractor.id, entity_label: contractor.name },
   499→   499→                    { entity_type: "customer", entity_id: acceptedScope.customer_id },
   500→   500→                    ...(acceptedScope.site_id ? [{ entity_type: "site", entity_id: acceptedScope.site_id }] : []),
   501→   501→                ],
   502→   502→            });
   503→   503→            return workOrderId;
   504→   504→        },
   505→   505→
   506→   506→        settleContractor: (params) => {
   507→   507→            assertRole(get().currentUser().role, ["Owner"], "settle contractors");
   508→   508→            const actor = get().currentUser();
   509→   509→            const state = get();
   510→   510→            const workOrder = state.db.workOrders.find((j: any) => j.id === params.workOrderId);
   511→   511→            if (!workOrder || !workOrder.contractor_id)
   512→   512→                return { settlementId: "" };
   513→   513→            const contractor = state.db.master.contractors.find((c: any) => c.id === workOrder.contractor_id);
   514→   514→            if (!contractor)
   515→   515→                return { settlementId: "" };
   516→   516→            const proof = contractorPaymentProofStatus(state.db, workOrder.id);
   517→   517→            if (!proof.ok)
   518→   518→                throw new Error(proof.reason);
   519→   519→            const advances = params.advancesPaid ??
   520→   520→                state.db.workOrderCostLines
   521→   521→                    .filter((c: any) => c.work_order_id === workOrder.id &&
   522→   522→                    c.type === "contractor" &&
   523→   523→                    c.source_kind === "contractor_payment")
   524→   524→                    .reduce((n: any, c: any) => n + c.amount, 0);
   525→   525→            const materialsIssued = params.materialsIssuedValue ?? 0;
   526→   526→            const recoveries = params.recoveries ?? 0;
   527→   527→            const completedPct = Math.max(0, Math.min(100, params.completedPct));
   528→   528→            const payable = Math.max(0, Math.round((completedPct / 100) * workOrder.value -
   529→   529→                advances -
   530→   530→                materialsIssued +
   531→   531→                recoveries));
   532→   532→            const settlementId = genId("sett");
   533→   533→            const settlementNo = `SET-${Date.now().toString(36).toUpperCase()}`;
   534→   534→            const now = nowIso();
   535→   535→            const threadId = state.openThreadFor("settlement", settlementId, `Settlement ${settlementNo} · ${contractor.name} · ${workOrder.work_order_no}`, [contractor.name]);
   536→   536→            let replacementJobId: string | undefined;
   537→   537→            commitState((s: any) => {
   538→   538→                const newSettlement: ContractorSettlement = {
   539→   539→                    id: settlementId,
   540→   540→                    settlement_no: settlementNo,
   541→   541→                    work_order_id: workOrder.id,
   542→   542→                    work_order_no: workOrder.work_order_no,
   543→   543→                    site_id: workOrder.site_id,
   544→   544→                    contractor_id: contractor.id,
   545→   545→                    contractor_name: contractor.name,
   546→   546→                    type: params.type || "abandonment",
   547→   547→                    completed_pct: completedPct,
   548→   548→                    contract_value: workOrder.value,
   549→   549→                    advances_paid: advances,
   550→   550→                    materials_issued_value: materialsIssued,
   551→   551→                    recoveries: recoveries,
   552→   552→                    payable_amount: payable,
   553→   553→                    reason: params.reason,
   554→   554→                    settled_at: now,
   555→   555→                    thread_id: threadId,
   556→   556→                    created_at: now,
   557→   557→                    updated_at: now,
   558→   558→                };
   559→   559→                const costLine: WorkOrderCostLine = {
   560→   560→                    id: genId("jcl"),
   561→   561→                    work_order_id: workOrder.id,
   562→   562→                    // CV-13: Classify contractor abandonment settlements as "contractor" (not "overhead")
   563→   563→                    // so the contractor cost KPI in P&L / site financials includes settlement payouts.
   564→   564→                    // computeJobPnL and siteFinancials (in selectors.ts) sum type "contractor" / "subcontract"
   565→   565→                    // for contractor cost; the settlement amount is now counted there. The `source_kind`
   566→   566→                    // remains "settlement" so the cost line's origin is still traceable.
   567→   567→                    type: "contractor",
   568→   568→                    description: `Settlement ${settlementNo} · ${contractor.name} · ${completedPct}% complete`,
   569→   569→                    amount: payable,
   570→   570→                    date: today(),
   571→   571→                    source_kind: "settlement",
   572→   572→                    source_id: settlementId,
   573→   573→                    vendor_id: contractor.id,
   574→   574→                    vendor_name: contractor.name,
   575→   575→                    created_at: now,
   576→   576→                };
   577→   577→                const updatedJobs = s.db.workOrders.map((j: any) => j.id === workOrder.id
   578→   578→                    ? {
   579→   579→                        ...j,
   580→   580→                        status: "abandoned" as const,
   581→   581→                        abandoned_at: now,
   582→   582→                        abandoned_reason: params.reason,
   583→   583→                        abandoned_contractor_id: contractor.id,
   584→   584→                        abandoned_contractor_name: contractor.name,
   585→   585→                        contractor_id: undefined,
   586→   586→                        contractor_name: undefined,
   587→   587→                        updated_at: now,
   588→   588→                    }
   589→   589→                    : j);
   590→   590→                return {
   591→   591→                    db: {
   592→   592→                        ...s.db,
   593→   593→                        contractorSettlements: [
   594→   594→                            newSettlement,
   595→   595→                            ...s.db.contractorSettlements,
   596→   596→                        ],
   597→   597→                        workOrderCostLines: [costLine, ...s.db.workOrderCostLines],
   598→   598→                        workOrders: updatedJobs,
   599→   599→                    },
   600→   600→                };
   601→   601→            });
   602→   602→            if (params.createReplacementJob) {
   603→   603→                const replId = genId("workOrder");
   604→   604→                const replNo = `${workOrder.work_order_no}-R`;
   605→   605→                const replThreadId = get().openThreadFor("workOrder", replId, `${replNo} · replacement for ${workOrder.work_order_no}`, [workOrder.customer_name || "Customer"]);
   606→   606→                replacementJobId = replId;
   607→   607→                const now2 = nowIso();
   608→   608→                commitState((s: any) => {
   609→   609→                    const replacement: WorkOrder = {
   610→   610→                        ...workOrder,
   611→   611→                        id: replId,
   612→   612→                        work_order_no: replNo,
   613→   613→                        status: "scheduled",
   614→   614→                        contractor_id: undefined,
   615→   615→                        contractor_name: undefined,
   616→   616→                        progress: 0,
   617→   617→                        start_date: today(),
   618→   618→                        actual_end: undefined,
   619→   619→                        abandoned_at: undefined,
   620→   620→                        abandoned_reason: undefined,
   621→   621→                        abandoned_contractor_id: undefined,
   622→   622→                        abandoned_contractor_name: undefined,
   623→   623→                        replacement_for_work_order_id: workOrder.id,
   624→   624→                        thread_id: replThreadId,
   625→   625→                        created_at: now2,
   626→   626→                        updated_at: now2,
   627→   627→                    };
   628→   628→                    const settlements = s.db.contractorSettlements.map((x: any) => x.id === settlementId
   629→   629→                        ? { ...x, replacement_work_order_id: replId }
   630→   630→                        : x);
   631→   631→                    return {
   632→   632→                        db: {
   633→   633→                            ...s.db,
   634→   634→                            workOrders: [replacement, ...s.db.workOrders],
   635→   635→                            contractorSettlements: settlements,
   636→   636→                        },
   637→   637→                    };
   638→   638→                });
   639→   639→                get().logAudit({
   640→   640→                    actor: actor.name,
   641→   641→                    actor_role: actor.role,
   642→   642→                    action: `Replacement workOrder ${replNo} created for abandoned ${workOrder.work_order_no}`,
   643→   643→                    entity_type: "workOrder",
   644→   644→                    entity_id: replId,
   645→   645→                    entity_label: replNo,
   646→   646→                    kind: "create",
   647→   647→                    cross_post: [
   648→   648→                        { entity_type: "workOrder", entity_id: workOrder.id, entity_label: workOrder.work_order_no },
   649→   649→                        ...(workOrder.site_id ? [{ entity_type: "site", entity_id: workOrder.site_id }] : []),
   650→   650→                        ...(workOrder.customer_id ? [{ entity_type: "customer", entity_id: workOrder.customer_id }] : []),
   651→   651→                        ...(workOrder.contractor_id ? [{ entity_type: "contractor", entity_id: workOrder.contractor_id, entity_label: workOrder.contractor_name }] : []),
   652→   652→                    ],
   653→   653→                });
   654→   654→            }
   655→   655→            get().addThreadReply(threadId, {
   656→   656→                author: actor.name,
   657→   657→                role: actor.role,
   658→   658→                body: `Settlement posted · ${completedPct}% complete · payable ${formatINR(payable)} (contract ${formatINR(workOrder.value)} − advances ${formatINR(advances)} − materials ${formatINR(materialsIssued)} + recoveries ${formatINR(recoveries)}). Reason: ${params.reason}${replacementJobId ? `. Replacement workOrder opened for new bidding.` : ""}`,
   659→   659→                kind: "decision",
   660→   660→            });
   661→   661→            get().logAudit({
   662→   662→                actor: actor.name,
   663→   663→                actor_role: actor.role,
   664→   664→                action: `Settled contractor ${contractor.name} on ${workOrder.work_order_no} for ${formatINR(payable)} (${completedPct}% complete)`,
   665→   665→                entity_type: "settlement",
   666→   666→                entity_id: settlementId,
   667→   667→                entity_label: settlementNo,
   668→   668→                kind: "decision",
   669→   669→                cross_post: [
   670→   670→                    { entity_type: "contractor", entity_id: contractor.id, entity_label: contractor.name },
   671→   671→                    { entity_type: "workOrder", entity_id: workOrder.id, entity_label: workOrder.work_order_no },
   672→   672→                    ...(workOrder.site_id ? [{ entity_type: "site", entity_id: workOrder.site_id }] : []),
   673→   673→                    ...(workOrder.customer_id ? [{ entity_type: "customer", entity_id: workOrder.customer_id }] : []),
   674→   674→                ],
   675→   675→            });
   676→   676→            return { settlementId, replacementJobId };
   677→   677→        },
   678→   678→
   679→   679→        createContractorRABill: (workOrderId, contractorId, amount, description, progressPct) => {
   680→   680→            assertRole(get().currentUser().role, ["Owner", "Finance", "Operations Manager"], "create contractor RA bills");
   681→   681→            const state = get();
   682→   682→            const actor = state.currentUser();
   683→   683→            const workOrder = state.db.workOrders.find((row: any) => row.id === workOrderId);
   684→   684→            const contractor = state.db.master.contractors.find((row: any) => row.id === contractorId);
   685→   685→            if (!workOrder ||
   686→   686→                !contractor ||
   687→   687→                workOrder.contractor_id !== contractor.id)
   688→   688→                throw new Error("RA bill must use the awarded contractor for this Work Order.");
   689→   689→            if (!Number.isFinite(amount) || amount <= 0)
   690→   690→                throw new Error("RA bill amount must be greater than zero.");
   691→   691→            const proof = contractorPaymentProofStatus(state.db, workOrderId);
   692→   692→            // CV-2: Relax the contractor-confirmation proof gate. The user wants flexibility over strictness —
   693→   693→            // previously a missing executionLog attachment hard-blocked ALL RA bill creation, killing the
   694→   694→            // contractor payment chain for any work order without a daily-log photo on file. Now we warn
   695→   695→            // (via a thread reply + audit log) but still let the business file the RA bill. The approval /
   696→   696→            // settlement actions keep the proof check so the final release still requires proof.
   697→   697→            const proofPending = !proof.ok;
   698→   698→            const accrued = state.db.contractorBills
   699→   699→                .filter((row: any) => row.work_order_id === workOrderId && row.status !== "held")
   700→   700→                .reduce((sum: any, row: any) => sum + row.amount, 0);
   701→   701→            // CV-1b: `??` does NOT treat 0 as nullish, so a stale contractor_award_amount of 0 (left over
   702→   702→            // from a placeholder bid) collapsed the entire RA-bill limit to 0 and blocked every positive
   703→   703→            // bill. Use a positive-check so a missing/zero award falls back to the work-order value.
   704→   704→            const contractLimit = (workOrder.contractor_award_amount && workOrder.contractor_award_amount > 0)
   705→   705→                ? workOrder.contractor_award_amount
   706→   706→                : workOrder.value;
   707→   707→            if (accrued + amount > contractLimit + 0.01)
   708→   708→                throw new Error("RA bill exceeds the contractor award / Work Order value. Create an approved variation before billing beyond the award.");
   709→   709→            const id = genId("cbill");
   710→   710→            const now = nowIso();
   711→   711→            const threadId = state.openThreadFor("settlement", id, `Contractor RA bill · ${workOrder.work_order_no} · ${contractor.name}`, [contractor.name, actor.name]);
   712→   712→            const bill: ContractorBill = {
   713→   713→                id,
   714→   714→                bill_no: `CTB-2026-${String(state.db.contractorBills.length + 1).padStart(3, "0")}`,
   715→   715→                ra_no: `RA-${String(state.db.contractorBills.filter((row: any) => row.work_order_id === workOrderId).length + 1).padStart(2, "0")}`,
   716→   716→                description,
   717→   717→                customer_id: workOrder.customer_id,
   718→   718→                site_id: workOrder.site_id,
   719→   719→                work_order_id: workOrder.id,
   720→   720→                work_required_id: workOrder.work_required_ids[0],
   721→   721→                area_ids: workOrder.area_ids,
   722→   722→                contractor_id: contractor.id,
   723→   723→                contractor_name: contractor.name,
   724→   724→                amount,
   725→   725→                paid_amount: 0,
   726→   726→                balance_amount: amount,
   727→   727→                status: "verified",
   728→   728→                progress_pct: progressPct ?? workOrder.progress,
   729→   729→                due_date: today(),
   730→   730→                verified_at: now,
   731→   731→                verified_by: actor.name,
   732→   732→                thread_id: threadId,
   733→   733→                created_at: now,
   734→   734→                updated_at: now,
   735→   735→            };
   736→   736→            // CV-2: Flag the bill as "proof pending" when no contractor confirmation was uploaded.
   737→   737→            // The bill is still created (verified) so the business can proceed, but the warning is visible
   738→   738→            // to anyone reviewing the thread / audit log.
   739→   739→            if (proofPending) {
   740→   740→                get().addThreadReply(threadId, {
   741→   741→                    author: actor.name,
   742→   742→                    role: actor.role,
   743→   743→                    body: `Warning: contractor confirmation proof not yet uploaded for ${workOrder.work_order_no}. RA bill ${bill.ra_no} created anyway (flexible mode). Upload proof via Sites & Execution → Execution Logs before releasing the final payment.`,
   744→   744→                    kind: "note",
   745→   745→                });
   746→   746→            }
   747→   747→            const costLine: WorkOrderCostLine = {
   748→   748→                id: genId("jcl"),
   749→   749→                work_order_id: workOrder.id,
   750→   750→                type: "contractor",
   751→   751→                description: `${contractor.name} — verified ${bill.ra_no} / ${bill.bill_no}: ${description}`,
   752→   752→                amount,
   753→   753→                date: now,
   754→   754→                source_kind: "bill",
   755→   755→                source_id: id,
   756→   756→                vendor_id: contractor.id,
   757→   757→                vendor_name: contractor.name,
   758→   758→                created_at: now,
   759→   759→            };
   760→   760→            commitState((s: any) => ({
   761→   761→                db: {
   762→   762→                    ...s.db,
   763→   763→                    contractorBills: [bill, ...s.db.contractorBills],
   764→   764→                    workOrderCostLines: [costLine, ...s.db.workOrderCostLines],
   765→   765→                },
   766→   766→            }));
   767→   767→            get().addThreadReply(threadId, {
   768→   768→                author: actor.name,
   769→   769→                role: actor.role,
   770→   770→                body: `Verified ${bill.ra_no} recorded for ${formatINR(amount)}. Finance may request one or more partial payments against its remaining balance.`,
   771→   771→                kind: "decision",
   772→   772→            });
   773→   773→            get().logAudit({
   774→   774→                actor: actor.name,
   775→   775→                actor_role: actor.role,
   776→   776→                action: `Verified contractor ${bill.ra_no} ${bill.bill_no} for ${workOrder.work_order_no} (${formatINR(amount)})`,
   777→   777→                entity_type: "workOrder",
   778→   778→                entity_id: workOrderId,
   779→   779→                entity_label: workOrder.work_order_no,
   780→   780→                kind: "create",
   781→   781→                cross_post: [
   782→   782→                    { entity_type: "contractorBill", entity_id: id, entity_label: bill.bill_no },
   783→   783→                    { entity_type: "contractor", entity_id: contractor.id, entity_label: contractor.name },
   784→   784→                    ...(workOrder.site_id ? [{ entity_type: "site", entity_id: workOrder.site_id }] : []),
   785→   785→                    ...(workOrder.customer_id ? [{ entity_type: "customer", entity_id: workOrder.customer_id }] : []),
   786→   786→                ],
   787→   787→            });
   788→   788→            // J: Recompute contractor performance after an RA bill is filed.
   789→   789→            // Best-effort — never throw.
   790→   790→            try { get().recomputeContractorPerformance(contractor.id); }
   791→   791→            catch (err) { console.warn("[createContractorRABill] recomputeContractorPerformance failed", err); }
   792→   792→            return id;
   793→   793→        },
   794→   794→
   795→   795→        requestContractorBillPayment: (billId, amount) => {
   796→   796→            assertRole(get().currentUser().role, ["Owner", "Finance", "Operations Manager"], "request contractor bill payments");
   797→   797→            const state = get();
   798→   798→            const actor = state.currentUser();
   799→   799→            const bill = state.db.contractorBills.find((row: any) => row.id === billId);
   800→   800→            if (!bill || bill.status === "held" || bill.status === "paid")
   801→   801→                throw new Error("A verified open contractor bill is required.");
   802→   802→            const committed = state.db.contractorPayments
   803→   803→                .filter((row: any) => row.contractor_bill_id === bill.id &&
   804→   804→                (row.status === "pending" || row.status === "approved"))
   805→   805→                .reduce((sum: any, row: any) => sum + row.amount, 0);
   806→   806→            const requestable = Math.max(0, bill.balance_amount - committed);
   807→   807→            if (!Number.isFinite(amount) ||
   808→   808→                amount <= 0 ||
   809→   809→                amount > requestable + 0.01)
   810→   810→                throw new Error(`Payment request must be within the unrequested contractor bill balance (${formatINR(requestable)}).`);
   811→   811→            const proof = contractorPaymentProofStatus(state.db, bill.work_order_id);
   812→   812→            if (!proof.ok)
   813→   813→                throw new Error(proof.reason);
   814→   814→            const policy = state.requiresApproval("contractor_payment", amount);
   815→   815→            const id = genId("cpay");
   816→   816→            const now = nowIso();
   817→   817→            const payment: ContractorPayment = {
   818→   818→                id,
   819→   819→                payment_no: `CP-2026-${String(state.db.contractorPayments.length + 1).padStart(3, "0")}`,
   820→   820→                contractor_bill_id: bill.id,
   821→   821→                work_order_id: bill.work_order_id,
   822→   822→                site_id: bill.site_id,
   823→   823→                contractor_id: bill.contractor_id,
   824→   824→                contractor_name: bill.contractor_name,
   825→   825→                amount,
   826→   826→                mode: "bank_transfer",
   827→   827→                reference: "",
   828→   828→                status: policy ? "pending" : "approved",
   829→   829→                thread_id: bill.thread_id,
   830→   830→                created_at: now,
   831→   831→                updated_at: now,
   832→   832→            };
   833→   833→            commitState((s: any) => ({
   834→   834→                db: {
   835→   835→                    ...s.db,
   836→   836→                    contractorPayments: [payment, ...s.db.contractorPayments],
   837→   837→                },
   838→   838→            }));
   839→   839→            if (policy) {
   840→   840→                const approvalId = genId("appr");
   841→   841→                commitState((s: any) => ({
   842→   842→                    db: {
   843→   843→                        ...s.db,
   844→   844→                        actions: [
   845→   845→                            {
   846→   846→                                id: approvalId,
   847→   847→                                title: `Approve contractor payment · ${bill.contractor_name} (${formatINR(amount)})`,
   848→   848→                                type: "contractor_payment",
   849→   849→                                status: "pending",
   850→   850→                                customer_id: bill.customer_id,
   851→   851→                                amount,
   852→   852→                                requested_by: actor.name,
   853→   853→                                due_date: today(),
   854→   854→                                linked_record_id: id,
   855→   855→                                linked_record_type: "contractor_payment",
   856→   856→                                created_at: now,
   857→   857→                            },
   858→   858→                            ...s.db.actions,
   859→   859→                        ],
   860→   860→                    },
   861→   861→                }));
   862→   862→                get().addTask({
   863→   863→                    title: `Approve contractor payment · ${bill.contractor_name} (${formatINR(amount)})`,
   864→   864→                    customer_id: bill.customer_id,
   865→   865→                    site_id: bill.site_id,
   866→   866→                    work_order_id: bill.work_order_id,
   867→   867→                    task_scope: "office",
   868→   868→                    task_type: "contractor_payment_approval",
   869→   869→                    assignee_name: policy.approver_name || "Owner",
   870→   870→                    auto_generated: true,
   871→   871→                    due_date: today(),
   872→   872→                });
   873→   873→            }
   874→   874→            get().addThreadReply(bill.thread_id || "", {
   875→   875→                author: actor.name,
   876→   876→                role: actor.role,
   877→   877→                body: `Payment request ${payment.payment_no} created for ${formatINR(amount)} against ${bill.bill_no}; remaining bill balance is ${formatINR(bill.balance_amount)}.`,
   878→   878→                kind: "decision",
   879→   879→            });
   880→   880→            return id;
   881→   881→        },
   882→   882→
   883→   883→        recordContractorPayment: (paymentId, mode, reference) => {
   884→   884→            assertRole(get().currentUser().role, ["Owner", "Finance"], "record contractor payments");
   885→   885→            const state = get();
   886→   886→            const actor = state.currentUser();
   887→   887→            const payment = state.db.contractorPayments.find((row: any) => row.id === paymentId);
   888→   888→            if (!payment)
   889→   889→                throw new Error("Contractor payment not found.");
   890→   890→            if (payment.status !== "approved")
   891→   891→                throw new Error("Contractor payment must be approved before it can be paid.");
   892→   892→            const bill = state.db.contractorBills.find((row: any) => row.id === payment.contractor_bill_id);
   893→   893→            if (!bill || bill.status === "held")
   894→   894→                throw new Error("Verified contractor bill is required before payment.");
   895→   895→            if (!reference.trim())
   896→   896→                throw new Error("A bank/UPI/cash reference is required for contractor payment.");
   897→   897→            if (payment.amount > bill.balance_amount + 0.01)
   898→   898→                throw new Error("Contractor payment exceeds the remaining RA bill balance.");
   899→   899→            const nextPaid = Math.round((bill.paid_amount + payment.amount) * 100) / 100;
   900→   900→            const billBalance = Math.max(0, Math.round((bill.amount - nextPaid) * 100) / 100);
   901→   901→            const billStatus: ContractorBill["status"] = billBalance === 0 ? "paid" : "partly_paid";
   902→   902→            const now = nowIso();
   903→   903→            commitState((s: any) => ({
   904→   904→                db: {
   905→   905→                    ...s.db,
   906→   906→                    contractorPayments: s.db.contractorPayments.map((row: any) => row.id === paymentId
   907→   907→                        ? {
   908→   908→                            ...row,
   909→   909→                            mode,
   910→   910→                            reference: reference.trim(),
   911→   911→                            status: "paid",
   912→   912→                            paid_at: today(),
   913→   913→                            approved_at: row.approved_at || now,
   914→   914→                            approved_by: row.approved_by || actor.name,
   915→   915→                            updated_at: now,
   916→   916→                        }
   917→   917→                        : row),
   918→   918→                    contractorBills: s.db.contractorBills.map((row: any) => row.id === bill.id
   919→   919→                        ? {
   920→   920→                            ...row,
   921→   921→                            paid_amount: nextPaid,
   922→   922→                            balance_amount: billBalance,
   923→   923→                            status: billStatus,
   924→   924→                            updated_at: now,
   925→   925→                        }
   926→   926→                        : row),
   927→   927→                },
   928→   928→            }));
   929→   929→            get().addThreadReply(payment.thread_id || bill.thread_id || "", {
   930→   930→                author: actor.name,
   931→   931→                role: actor.role,
   932→   932→                body: `Contractor payment ${payment.payment_no} recorded: ${formatINR(payment.amount)} via ${mode}. Ref: ${reference.trim()}. Bill balance: ${formatINR(billBalance)}.`,
   933→   933→                kind: "decision",
   934→   934→            });
   935→   935→            get().logAudit({
   936→   936→                actor: actor.name,
   937→   937→                actor_role: actor.role,
   938→   938→                action: `Paid contractor ${payment.contractor_name || bill.contractor_name} ${formatINR(payment.amount)} for ${bill.bill_no}`,
   939→   939→                entity_type: "workOrder",
   940→   940→                entity_id: payment.work_order_id,
   941→   941→                kind: "receive",
   942→   942→                cross_post: [
   943→   943→                    { entity_type: "contractorPayment", entity_id: paymentId, entity_label: payment.payment_no },
   944→   944→                    { entity_type: "contractorBill", entity_id: bill.id, entity_label: bill.bill_no },
   945→   945→                    ...(payment.contractor_id ? [{ entity_type: "contractor", entity_id: payment.contractor_id, entity_label: payment.contractor_name || bill.contractor_name }] : []),
   946→   946→                    ...(payment.site_id ? [{ entity_type: "site", entity_id: payment.site_id }] : []),
   947→   947→                ],
   948→   948→            });
   949→   949→        },
   950→   950→
   951→   951→        approveContractorPayment: (approvalId) => {
   952→   952→            assertRole(get().currentUser().role, ["Owner"], "approve contractor payments");
   953→   953→            const actor = get().currentUser();
   954→   954→            const approval = get().db.actions.find((a: any) => a.id === approvalId);
   955→   955→            if (!approval ||
   956→   956→                approval.linked_record_type !== "contractor_payment" ||
   957→   957→                !approval.linked_record_id)
   958→   958→                return;
   959→   959→            const payment = get().db.contractorPayments.find((row: any) => row.id === approval.linked_record_id);
   960→   960→            if (!payment)
   961→   961→                throw new Error("Contractor payment was not found for approval.");
   962→   962→            const proof = contractorPaymentProofStatus(get().db, payment.work_order_id);
   963→   963→            if (!proof.ok)
   964→   964→                throw new Error(proof.reason);
   965→   965→            commitState((s: any) => ({
   966→   966→                db: {
   967→   967→                    ...s.db,
   968→   968→                    contractorPayments: s.db.contractorPayments.map((row: any) => row.id === payment.id
   969→   969→                        ? {
   970→   970→                            ...row,
   971→   971→                            status: "approved",
   972→   972→                            approved_at: nowIso(),
   973→   973→                            approved_by: actor.name,
   974→   974→                            updated_at: nowIso(),
   975→   975→                        }
   976→   976→                        : row),
   977→   977→                },
   978→   978→            }));
   979→   979→            get().addThreadReply(payment.thread_id || "", {
   980→   980→                author: actor.name,
   981→   981→                role: actor.role,
   982→   982→                body: `Payment ${payment.payment_no} approved. Finance must record the actual payment mode and reference before settlement.`,
   983→   983→                kind: "decision",
   984→   984→            });
   985→   985→            get().logAudit({
   986→   986→                actor: actor.name,
   987→   987→                actor_role: actor.role,
   988→   988→                action: `Approved contractor payment ${payment.payment_no} for ${formatINR(payment.amount)}`,
   989→   989→                entity_type: "workOrder",
   990→   990→                entity_id: payment.work_order_id,
   991→   991→                kind: "approve",
   992→   992→                cross_post: [
   993→   993→                    { entity_type: "contractorPayment", entity_id: payment.id, entity_label: payment.payment_no },
   994→   994→                    ...(payment.contractor_bill_id ? [{ entity_type: "contractorBill", entity_id: payment.contractor_bill_id }] : []),
   995→   995→                    ...(payment.contractor_id ? [{ entity_type: "contractor", entity_id: payment.contractor_id, entity_label: payment.contractor_name }] : []),
   996→   996→                    ...(payment.site_id ? [{ entity_type: "site", entity_id: payment.site_id }] : []),
   997→   997→                ],
   998→   998→            });
   999→   999→        },
  1000→  1000→
  1001→  1001→        accrueCommission: (workOrderId, quotationId, sourcePartnerId) => {
  1002→  1002→            const state = get();
  1003→  1003→            const workOrder = state.db.workOrders.find((j: any) => j.id === workOrderId);
  1004→  1004→            const partner = state.db.master.sourcePartners.find((p: any) => p.id === sourcePartnerId);
  1005→  1005→            if (!workOrder || !partner)
  1006→  1006→                return;
  1007→  1007→            // I: Look up the commissionRules master via Agent A's canonical
  1008→  1008→            // `findCommissionRule(db, sourcePartnerId, workCategoryId)` helper
  1009→  1009→            // (exported from masters.ts). The match priority is:
  1010→  1010→            //   1. partner-specific + applies_to="category" + category_id match
  1011→  1011→            //   2. partner-specific + applies_to="workOrder"
  1012→  1012→            //   3. partner-specific + applies_to="all"
  1013→  1013→            //   4. global applies_to="all" (no source_partner_id)
  1014→  1014→            // Only when no rule matches do we fall back to partner.commission_pct
  1015→  1015→            // (and finally to the historical 5% default). Previously this code
  1016→  1016→            // inlined a partial lookup that missed the workOrder priority and the
  1017→  1017→            // global fallback — so a rule saved with applies_to="workOrder" or a
  1018→  1018→            // global catch-all was silently ignored. Using the shared helper keeps
  1019→  1019→            // accruals consistent with the commission-rules master view and the
  1020→  1020→            // MastersSalesOpsModule banner.
  1021→  1021→            const workCategoryId = state.db.workRequired
  1022→  1022→                .find((w: any) => workOrder.work_required_ids.includes(w.id))?.work_category_id;
  1023→  1023→            const matchedRule = findCommissionRule(state.db, sourcePartnerId, workCategoryId);
  1024→  1024→            const matchedLabel = matchedRule
  1025→  1025→                ? `${matchedRule.applies_to}${matchedRule.category_id ? ` · ${matchedRule.category_id}` : ""}${matchedRule.source_partner_id ? ` · partner ${matchedRule.source_partner_name || matchedRule.source_partner_id}` : " · global"}`
  1026→  1026→                : `fallback: partner.commission_pct || 5`;
  1027→  1027→            const rate = matchedRule?.rate_pct ?? partner.commission_pct ?? 5;
  1028→  1028→            const id = genId("comm");
  1029→  1029→            const commissionNo = `COMM-${Date.now().toString().slice(-5)}`;
  1030→  1030→            const threadId = state.openThreadFor("commission", id, `${commissionNo} · ${partner.name}`, [partner.name, "Owner"]);
  1031→  1031→            const comm: Commission = {
  1032→  1032→                id,
  1033→  1033→                commission_no: commissionNo,
  1034→  1034→                source_partner_id: partner.id,
  1035→  1035→                source_partner_name: partner.name,
  1036→  1036→                customer_id: workOrder.customer_id,
  1037→  1037→                site_id: workOrder.site_id,
  1038→  1038→                work_order_id: workOrderId,
  1039→  1039→                work_order_no: workOrder.work_order_no,
  1040→  1040→                quotation_id: quotationId,
  1041→  1041→                base_amount: workOrder.value,
  1042→  1042→                rate_pct: rate,
  1043→  1043→                amount: Math.round((workOrder.value * rate) / 100),
  1044→  1044→                status: "accrued",
  1045→  1045→                accrued_at: nowIso(),
  1046→  1046→                thread_id: threadId,
  1047→  1047→                created_at: nowIso(),
  1048→  1048→                updated_at: nowIso(),
  1049→  1049→            };
  1050→  1050→            commitState((s: any) => ({
  1051→  1051→                db: { ...s.db, commissions: [comm, ...s.db.commissions] },
  1052→  1052→            }));
  1053→  1053→            // I: Document the rule lookup in the audit log so commission
  1054→  1054→            // accruals are traceable to the rule (or to the fallback).
  1055→  1055→            get().logAudit({
  1056→  1056→                actor: "System",
  1057→  1057→                action: `Accrued commission ${commissionNo} for ${partner.name} on ${workOrder.work_order_no} — ${rate}% of ${formatINR(workOrder.value)} = ${formatINR(comm.amount)} (rule: ${matchedLabel})`,
  1058→  1058→                entity_type: "commission",
  1059→  1059→                entity_id: id,
  1060→  1060→                entity_label: commissionNo,
  1061→  1061→                kind: "system",
  1062→  1062→                source_module: "contractors",
  1063→  1063→                cross_post: [
  1064→  1064→                    { entity_type: "workOrder", entity_id: workOrderId, entity_label: workOrder.work_order_no },
  1065→  1065→                    ...(workOrder.site_id ? [{ entity_type: "site", entity_id: workOrder.site_id }] : []),
  1066→  1066→                    { entity_type: "sourcePartner", entity_id: partner.id, entity_label: partner.name },
  1067→  1067→                    ...(matchedRule ? [{ entity_type: "commissionRule", entity_id: matchedRule.id }] : []),
  1068→  1068→                ],
  1069→  1069→            });
  1070→  1070→        },
  1071→  1071→
  1072→  1072→        payCommission: (id) => {
  1073→  1073→            const actor = get().currentUser();
  1074→  1074→            const commission = get().db.commissions.find((row: any) => row.id === id);
  1075→  1075→            const threadId = commission?.thread_id ||
  1076→  1076→                get().openThreadFor("commission", id, `${commission?.commission_no || id} · ${commission?.source_partner_name || ""}`, [commission?.source_partner_name || "Owner"]);
  1077→  1077→            commitState((s: any) => ({
  1078→  1078→                db: {
  1079→  1079→                    ...s.db,
  1080→  1080→                    commissions: s.db.commissions.map((c: any) => c.id === id
  1081→  1081→                        ? {
  1082→  1082→                            ...c,
  1083→  1083→                            status: "paid",
  1084→  1084→                            paid_date: today(),
  1085→  1085→                            thread_id: c.thread_id || threadId,
  1086→  1086→                            updated_at: nowIso(),
  1087→  1087→                        }
  1088→  1088→                        : c),
  1089→  1089→                },
  1090→  1090→            }));
  1091→  1091→            get().addThreadReply(threadId, {
  1092→  1092→                author: actor.name,
  1093→  1093→                role: actor.role,
  1094→  1094→                body: `Commission ${commission?.commission_no || id} marked paid.`,
  1095→  1095→                kind: "decision",
  1096→  1096→            });
  1097→  1097→        },
  1098→  1098→
  1099→  1099→        // J: Recompute a contractor's reliability_score, on_time_pct, and
  1100→  1100→        // rating from actual RA-bill + payment performance + execution-log
  1101→  1101→        // on-time filing. Previously these were static master fields set at
  1102→  1102→        // contractor creation and never updated — so a contractor with 50
  1103→  1103→        // settled RA bills kept whatever score was entered at creation. Now
  1104→  1104→        // we derive a 0-100 score from:
  1105→  1105→        //   • on-time completion: work orders whose actual_end <= expected_end
  1106→  1106→        //   • RA-bill settled vs disputed rate
  1107→  1107→        //   • disputed-bill penalty
  1108→  1108→        // We write the recomputed fields back to the contractor master so the
  1109→  1109→        // ContractorPerformanceModule leaderboard reflects reality.
  1110→  1110→        recomputeContractorPerformance: (contractorId) => {
  1111→  1111→            const state = get();
  1112→  1112→            const actor = state.currentUser();
  1113→  1113→            const contractor = state.db.master.contractors.find((c: any) => c.id === contractorId);
  1114→  1114→            if (!contractor)
  1115→  1115→                throw new Error("Contractor not found.");
  1116→  1116→            const contractorWOs = state.db.workOrders.filter((wo: any) => wo.contractor_id === contractorId);
  1117→  1117→            // On-time completion: completed WOs with both dates set.
  1118→  1118→            const completedWOs = contractorWOs.filter((wo: any) => wo.actual_end && wo.expected_end);
  1119→  1119→            const onTimeWOs = completedWOs.filter((wo: any) => wo.actual_end <= wo.expected_end).length;
  1120→  1120→            const onTimePct = completedWOs.length > 0
  1121→  1121→                ? Math.round((onTimeWOs / completedWOs.length) * 100)
  1122→  1122→                : 0;
  1123→  1123→            // RA-bill settled vs disputed rate.
  1124→  1124→            const contractorBills = state.db.contractorBills.filter((b: any) => b.contractor_id === contractorId && b.status !== "held");
  1125→  1125→            const settledBills = contractorBills.filter((b: any) => b.status === "paid" || b.status === "partly_paid").length;
  1126→  1126→            const disputedBills = contractorBills.filter((b: any) => b.status === "disputed").length;
  1127→  1127→            const settleRate = contractorBills.length > 0
  1128→  1128→                ? Math.round((settledBills / contractorBills.length) * 100)
  1129→  1129→                : 100;
  1130→  1130→            // Composite reliability score.
  1131→  1131→            const disputePenalty = Math.min(30, disputedBills * 10);
  1132→  1132→            const reliabilityScore = Math.max(0, Math.min(100, Math.round(onTimePct * 0.55 + settleRate * 0.45) - disputePenalty));
  1133→  1133→            const rating = reliabilityScore >= 90 ? 5
  1134→  1134→                : reliabilityScore >= 75 ? 4
  1135→  1135→                    : reliabilityScore >= 60 ? 3
  1136→  1136→                        : reliabilityScore >= 40 ? 2
  1137→  1137→                            : 1;
  1138→  1138→            commitState((s: any) => ({
  1139→  1139→                db: {
  1140→  1140→                    ...s.db,
  1141→  1141→                    master: {
  1142→  1142→                        ...s.db.master,
  1143→  1143→                        contractors: s.db.master.contractors.map((c: any) => c.id === contractorId
  1144→  1144→                            ? {
  1145→  1145→                                ...c,
  1146→  1146→                                reliability_score: reliabilityScore,
  1147→  1147→                                on_time_pct: onTimePct,
  1148→  1148→                                rating,
  1149→  1149→                                performance_recomputed_at: nowIso(),
  1150→  1150→                            }
  1151→  1151→                            : c),
  1152→  1152→                    },
  1153→  1153→                },
  1154→  1154→            }));
  1155→  1155→            get().logAudit({
  1156→  1156→                actor: actor.name,
  1157→  1157→                actor_role: actor.role,
  1158→  1158→                action: `Recomputed contractor performance for ${contractor.name}: reliability=${reliabilityScore}, on-time=${onTimePct}%, rating=${rating} (${contractorWOs.length} WOs, ${contractorBills.length} bills, ${disputedBills} disputed)`,
  1159→  1159→                entity_type: "contractor",
  1160→  1160→                entity_id: contractorId,
  1161→  1161→                entity_label: contractor.name,
  1162→  1162→                kind: "system",
  1163→  1163→                source_module: "contractors",
  1164→  1164→            });
  1165→  1165→        },
  1166→  1166→    };
  1167→  1167→}
  1168→  1168→